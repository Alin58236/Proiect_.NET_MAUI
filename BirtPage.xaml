<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="beerTMobile.BirtPage"
             Title="BirtPage">
    <ContentPage.Content>
        <StackLayout Margin="20">
            <Editor Placeholder="Nume Birt" Margin="20" Text="{Binding BirtName}" HeightRequest="50" />
            <Editor Placeholder="Adresa Birt" Margin="20" Text="{Binding Adresa}" HeightRequest="50" />
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Text="Salveaza Birt" Grid.Column="0" MaximumWidthRequest="200" Clicked="OnSaveButtonClicked" />
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>